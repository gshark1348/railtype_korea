# RAILTYPE KOREA v28.1 · 역·열차·환승 위치 동기화 테스트

## 수정 기준

`state.targetIndex`를 다음 네 요소의 공통 기준으로 사용합니다.

1. 타자 UI의 현재 입력 대상역
2. SVG 지도에서 `is-current`로 표시되는 역 노드
3. 열차와 진행 선로가 최종적으로 도달하는 역 위치
4. 무한모드 환승 버튼과 `TAB`이 판정하는 현재역

## 일반모드 브라우저 회귀 테스트

- 3호선 기본 코스 시작 시 입력 대상 `대화`, 현재 노드 `0`, 열차 진행 위치 `0`: **PASS**
- `대화` 정답 입력 직후 UI가 `주엽`으로 전환: **PASS**
- 애니메이션 중 현재 노드는 목적지 `주엽`으로 표시: **PASS**
- 애니메이션 종료 후 열차 진행 위치 `1`과 현재 노드 인덱스 `1` 일치: **PASS**
- JavaScript 런타임 오류: **0개**

## 무한모드 현재역·환승 테스트

### 출발역 동기화

- 수인분당선 선정릉 출발 시 입력 대상: `선정릉`
- 무한모드 현재 여정 표시: `수인분당선 · 인천행 · 선정릉`
- 환승 안내 기준역: `선정릉 · 9호선`
- SVG 현재 노드 인덱스와 열차 진행 위치 일치: **PASS**

### 다음 역 이동

- `선정릉` 정답 입력 직후 입력 대상이 `선릉`으로 전환: **PASS**
- 이동 중 환승 버튼 비활성화: **PASS**
- 이동 종료 후 현재 여정과 환승 안내가 모두 `선릉` 기준으로 변경: **PASS**
- 열차 진행 위치 `1`과 현재 노드 인덱스 `1` 일치: **PASS**

### 환승 후 동기화

- 선릉에서 `TAB` 입력 시 2호선으로 즉시 환승: **PASS**
- 환승 후 입력 대상: `삼성`
- 현재 여정 표시: `2호선 · 외선순환 · 삼성`
- 환승 후 열차 진행 위치와 현재 노드 인덱스 일치: **PASS**
- 직전 역인 선릉을 현재역으로 잘못 사용하는 현상: **재현되지 않음**

## 정적 검사

- `node --check js/app.js`: **PASS**
- `index.html`의 앱 캐시 버전: `28.1.0`
- 변경된 로컬 파일 참조 누락: **0개**

> 브라우저 검사는 외부 Supabase 요청을 제외한 독립 환경에서 일반모드와 무한모드 상태 전환을 확인했습니다.
