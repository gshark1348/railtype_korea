# RAILTYPE KOREA v26.1 · 위치 보정 테스트

## 자동 검사 결과

| 항목 | 결과 |
|---|---:|
| 대한민국 경계 GeoJSON 파싱 | **PASS** |
| 대한민국 경계 위상 유효성 | **PASS** |
| JavaScript 경계와 GeoJSON 파트 수 | **6 / 6** |
| KTX 호남 JS/CSV 역사 수 | **10 / 10** |
| KTX 호남 JS/CSV 좌표 일치 | **PASS** |
| 호남축 경계 내부 역사 | **10 / 10** |
| 경부축 경계 내부 역사 | **11 / 11** |
| 호남축 SVG 1200×760 내부 노드 | **10 / 10** |
| 최대 역사 좌표 조정 | **85.5 m** |
| 전체 JavaScript 구문 검사 | **PASS** |
| `index.html` 로컬 참조 검사 | **누락 0개** |
| v26.1 캐시 무효화 쿼리 | **PASS** |

## 런타임 데이터 검사

Node.js의 격리된 VM에서 다음 파일을 순서대로 로드했습니다.

- `js/ktx-honam-data.js`
- `js/ktx-gyeongbu-data.js`
- `js/national-boundary.js`

호남 10개 역, 경부 11개 역, 전국 경계 6개 파트와 자동 투영용 2개 파트가 정상 등록되는 것을 확인했습니다.

## 지도 투영 검사

실제 `app.js`와 동일한 `1200 × 760`, `paddingX=58`, `paddingY=50` 투영 계산을 재현했습니다. 보정된 호남축 10개 역 노드는 모두 SVG 범위 안에 위치하며 북쪽의 용산에서 남서쪽의 목포까지 순서대로 이어집니다.

## 브라우저 검사 범위

현재 실행 환경의 브라우저 보안 정책이 로컬 HTTP 및 `file://` 페이지 접근을 차단해 자동 클릭 기반 스크린샷 검사는 수행하지 못했습니다. 대신 JavaScript 구문, 데이터 로딩, HTML 참조, GeoJSON 위상, 좌표 일치와 실제 투영식을 각각 독립적으로 검증했습니다.
